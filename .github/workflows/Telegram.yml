name: 📥 Telegram Chat Downloader

on:
  workflow_dispatch:
    inputs:
      api_id:
        description: '🔑 API ID'
        required: true
        type: string
      api_hash:
        description: '🔐 API HASH'
        required: true
        type: string
      string_session:
        description: '📱 String Session'
        required: true
        type: string
      chat_target:
        description: '🎯 هدف (برای Saved Messages: me)'
        required: true
        type: string
        default: 'me'
      hashtag_filter:
        description: '🏷️ هشتگ مورد نظر (خالی = همه)'
        required: false
        type: string
        default: ''
      start_message_id:
        description: '🔢 شروع از Message ID (0 = ابتدا، برای جدیدترین‌ها عدد بزرگ بگذار)'
        required: false
        default: '0'
        type: string
      zip_password:
        description: '🔒 رمز فایل‌های زیپ (اختیاری)'
        required: false
        default: ''
        type: string

jobs:
  backup:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - run: pip install telethon

      - name: Get last message ID
        env:
          API_ID: ${{ github.event.inputs.api_id }}
          API_HASH: ${{ github.event.inputs.api_hash }}
          STRING_SESSION: ${{ github.event.inputs.string_session }}
          CHAT: ${{ github.event.inputs.chat_target }}
        run: |
          cat > get_last_id.py << 'EOF'
          import asyncio
          from telethon import TelegramClient
          from telethon.sessions import StringSession
          import os

          async def main():
              API_ID = int(os.environ['API_ID'])
              API_HASH = os.environ['API_HASH']
              STRING_SESSION = os.environ['STRING_SESSION']
              CHAT = os.environ.get('CHAT', 'me')
              
              async with TelegramClient(StringSession(STRING_SESSION), API_ID, API_HASH) as client:
                  me = await client.get_me()
                  print(f"✅ {me.first_name}")
                  
                  entity = await client.get_entity(CHAT)
                  print(f"🎯 {entity.title if hasattr(entity,'title') else entity.id}")
                  
                  last_msg = await client.get_messages(entity, limit=1)
                  if last_msg:
                      last_id = last_msg[0].id
                      print("")
                      print("="*50)
                      print(f"📊 آخرین Message ID در این چت: {last_id}")
                      print(f"💡 برای دانلود فقط جدیدترین‌ها، start_message_id را {last_id - 1000} قرار بده")
                      print(f"💡 برای دانلود از یک نقطه خاص، start_message_id را همان عدد وارد کن")
                      print("="*50)
                      
                      # ذخیره در فایل برای مرحله بعد
                      with open('/tmp/last_id.txt', 'w') as f:
                          f.write(str(last_id))
                  
          asyncio.run(main())
          EOF
          python get_last_id.py

      - name: Create download script
        run: |
          cat > download.py << 'EOF'
          import os, re, asyncio
          from telethon import TelegramClient
          from telethon.sessions import StringSession

          API_ID = int(os.environ['API_ID'])
          API_HASH = os.environ['API_HASH']
          STRING_SESSION = os.environ['STRING_SESSION']
          CHAT = os.environ.get('CHAT', 'me')
          HASHTAG_FILTER = os.environ.get('HASHTAG_FILTER', '').strip()
          START_ID = int(os.environ.get('START_ID', '0'))

          BASE = "saved_files"
          used_names = {}

          print("="*50)
          print("Telegram Video Downloader")
          print("="*50)
          print(f"Chat target: {CHAT}")
          if HASHTAG_FILTER:
              print(f"🏷️  Hashtag filter: #{HASHTAG_FILTER}")
          else:
              print(f"🏷️  Hashtag filter: ALL")
          print(f"Start Message ID: {START_ID}")
          print("="*50)
          print("")

          def remove_emojis(t):
              if not t: return ""
              t = re.sub(r'[⚛️😱👤🆔▶️◀️✅🔹🔸📢💡📌📺🔗📱]', '', t)
              t = re.sub(r'[\U00010000-\U0010ffff]', '', t)
              return t.strip()

          def clean_folder(t):
              if not t: return ""
              t = remove_emojis(t)
              t = re.sub(r'[^\w\s\u0600-\u06FF]', '', t)
              t = t.strip().replace(' ', '_')
              t = re.sub(r'_+', '_', t)
              return t[:50]

          def clean_file(t):
              if not t: return ""
              t = remove_emojis(t)
              t = re.sub(r'[\\/*?:"<>|]', '', t)
              t = t.strip().replace(' ', '_')
              t = re.sub(r'_+', '_', t)
              return t[:80]

          def extract_hashtags(text):
              if not text:
                  return []
              return re.findall(r'#(\w+)', text)

          def has_matching_hashtag(text):
              if not HASHTAG_FILTER:
                  return True
              hashtags = extract_hashtags(text)
              return any(h.lower() == HASHTAG_FILTER.lower() for h in hashtags)

          def get_folder_and_name(msg):
              text = msg.text or msg.message or ""
              if not text:
                  return '_no_tag', None
              lines = text.strip().split('\n')
              
              folder_parts = []
              if lines:
                  folder_parts.append(clean_folder(lines[0]))
              for line in lines[1:]:
                  if 'مدرس' in line: break
                  cl = remove_emojis(line)
                  if cl.startswith('دوره'):
                      folder_parts.append(clean_folder(cl))
              folder = '_'.join(folder_parts) if folder_parts else '_no_tag'
              
              file_parts = []
              for i, line in enumerate(lines):
                  if 'مدرس' in line: break
                  if i == 0: continue
                  cl = remove_emojis(line)
                  if cl and not cl.startswith('دوره'):
                      file_parts.append(clean_file(cl))
              fname = '_'.join(file_parts) if file_parts else None
              return folder, fname

          async def main():
              async with TelegramClient(StringSession(STRING_SESSION), API_ID, API_HASH) as client:
                  me = await client.get_me()
                  print(f"✅ Logged in as: {me.first_name}")
                  entity = await client.get_entity(CHAT)
                  print(f"🎯 Target: {entity.title if hasattr(entity,'title') else entity.id}")
                  print("")
                  
                  os.makedirs(BASE, exist_ok=True)
                  new = 0
                  failed = 0
                  checked = 0
                  video_count = 0
                  skipped = 0
                  
                  if HASHTAG_FILTER:
                      print(f"🏷️  Filtering videos with hashtag: #{HASHTAG_FILTER}")
                  print("🎬 Downloading VIDEO files...")
                  print("")
                  
                  async for msg in client.iter_messages(entity, min_id=START_ID, reverse=True):
                      checked += 1
                      if checked % 500 == 0:
                          print(f"📊 Processed {checked} msgs | Videos: {video_count} | Downloaded: {new} | Failed: {failed}")
                      
                      if not msg.video:
                          continue
                      
                      video_count += 1
                      text = msg.text or msg.message or ""
                      
                      if not has_matching_hashtag(text):
                          skipped += 1
                          continue
                      
                      folder, fname = get_folder_and_name(msg)
                      if not fname:
                          orig = getattr(msg.file, 'name', None)
                          if orig:
                              fname = clean_file(os.path.splitext(orig)[0])
                          else:
                              fname = f"video_{msg.id}"
                      
                      if fname in used_names:
                          used_names[fname] += 1
                          fname = f"{fname}_{used_names[fname]}"
                      else:
                          used_names[fname] = 1
                      
                      fname = fname + '.mp4'
                      path = os.path.join(BASE, folder, fname)
                      os.makedirs(os.path.dirname(path), exist_ok=True)
                      
                      if not os.path.exists(path):
                          try:
                              await msg.download_media(file=path)
                              new += 1
                              print(f"✅ [{new}] {folder}/{fname} (ID: {msg.id})")
                              await asyncio.sleep(1)
                          except Exception as e:
                              failed += 1
                              if "too long" in str(e):
                                  short = f"video_{msg.id}.mp4"
                                  short_path = os.path.join(BASE, folder, short)
                                  try:
                                      await msg.download_media(file=short_path)
                                      new += 1
                                      print(f"✅ [{new}] {folder}/{short} (shortened) (ID: {msg.id})")
                                      await asyncio.sleep(1)
                                  except:
                                      print(f"❌ Failed msg {msg.id}")
                              else:
                                  print(f"❌ Failed msg {msg.id}: {str(e)[:50]}")
                      else:
                          print(f"⏩ Already exists: {fname}")
                  
                  print("")
                  print("="*50)
                  print(f"🎉 Complete!")
                  print(f"📊 Total messages checked: {checked}")
                  print(f"🎬 Videos found: {video_count}")
                  if HASHTAG_FILTER:
                      print(f"🏷️  Videos with #{HASHTAG_FILTER}: {new + failed}")
                      print(f"⏭️  Skipped: {skipped}")
                  print(f"💾 Downloaded: {new}")
                  print(f"❌ Failed: {failed}")
                  print("="*50)

          asyncio.run(main())
          EOF

      - name: Run download script
        env:
          API_ID: ${{ github.event.inputs.api_id }}
          API_HASH: ${{ github.event.inputs.api_hash }}
          STRING_SESSION: ${{ github.event.inputs.string_session }}
          CHAT: ${{ github.event.inputs.chat_target }}
          HASHTAG_FILTER: ${{ github.event.inputs.hashtag_filter }}
          START_ID: ${{ github.event.inputs.start_message_id }}
        run: python download.py

      - name: Zip and split folders
        if: success()
        env:
          ZIP_PASSWORD: ${{ github.event.inputs.zip_password }}
        run: |
          BASE_DIR="saved_files"
          SPLIT_SIZE=45
          REPO_OWNER="${{ github.repository_owner }}"
          REPO_NAME="${{ github.event.repository.name }}"
          BRANCH="${GITHUB_REF_NAME}"

          if [ ! -d "$BASE_DIR" ]; then
            echo "No saved_files directory. Exiting."
            exit 0
          fi

          urlencode() { python3 -c "import urllib.parse,sys; print(urllib.parse.quote(sys.argv[1], safe=''))" "$1"; }

          for folder in "$BASE_DIR"/*/ ; do
            [ -d "$folder" ] || continue
            foldername=$(basename "$folder")
            echo "============================================"
            echo "📦 Processing folder: $foldername"
            echo "============================================"

            cd "$BASE_DIR"
            rm -f "${foldername}.zip" "${foldername}.z"* 2>/dev/null || true
            if [ -n "$ZIP_PASSWORD" ]; then
              zip -r -P "$ZIP_PASSWORD" -s ${SPLIT_SIZE}m "${foldername}.zip" "$foldername"
            else
              zip -r -s ${SPLIT_SIZE}m "${foldername}.zip" "$foldername"
            fi
            rm -rf "$foldername"
            cd ../..

            final_readme="$BASE_DIR/${foldername}_README.md"
            echo "# Archive: $foldername" > "$final_readme"
            echo "" >> "$final_readme"
            echo "| # | Part | Download |" >> "$final_readme"
            echo "|---|------|----------|" >> "$final_readme"
            link_counter=1
            for part in $(ls "$BASE_DIR/${foldername}.zip" "$BASE_DIR/${foldername}.z"* 2>/dev/null | sort); do
              part_basename=$(basename "$part")
              part_encoded=$(urlencode "$part_basename")
              raw_link="https://github.com/${REPO_OWNER}/${REPO_NAME}/raw/${BRANCH}/saved_files/${part_encoded}"
              echo "| $link_counter | \`$part_basename\` | [Download]($raw_link) |" >> "$final_readme"
              link_counter=$((link_counter+1))
            done
            echo "✅ $foldername → $((link_counter-1)) parts"
          done

      - name: Incremental Commit & Push
        if: success()
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          REPO_OWNER="${{ github.repository_owner }}"
          REPO_NAME="${{ github.event.repository.name }}"

          echo "=========================================="
          echo "Incremental Push Mode"
          echo "=========================================="

          BACKUP_DIR="saved_files"

          if [ ! -d "$BACKUP_DIR" ] || [ -z "$(ls -A "$BACKUP_DIR")" ]; then
            echo "No files to push. Exiting."
            exit 0
          fi

          git fetch origin "$BRANCH"
          git reset --hard origin/"$BRANCH"

          mkdir -p saved_files
          cp -r "$BACKUP_DIR"/* saved_files/

          git add -f saved_files/
          git diff --cached --quiet || git commit -m "Video backup $(date -u '+%Y-%m-%d %H:%M:%S') [skip ci]"

          PUSH_RETRY=0
          while [ $PUSH_RETRY -lt 10 ]; do
            PUSH_RETRY=$((PUSH_RETRY + 1))
            if timeout 300 git push origin HEAD:"$BRANCH"; then
              echo "Push successful!"
              break
            else
              echo "Push failed, retry $PUSH_RETRY/10..."
              sleep 5
              git fetch origin "$BRANCH"
              git reset --hard origin/"$BRANCH"
              mkdir -p saved_files
              cp -r "$BACKUP_DIR"/* saved_files/
              git add -f saved_files/
              git diff --cached --quiet || git commit -m "Video backup $(date -u '+%Y-%m-%d %H:%M:%S') [skip ci]"
            fi
          done

          echo "Push process completed!"
